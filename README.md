# go-concurrency-locks
Program to demontrate how mutex.Lock() and mutex.Unclock() works. Also the working of functions waitgroup.Add(),waitgroup.Done() and waitgroup.Wait().
# run
Run command go run main.go
